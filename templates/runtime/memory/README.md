# Daily Memory

This directory stores runtime daily memory files such as `YYYY-MM-DD.md`.

Daily memory should capture short-lived operational context such as:

- key tasks handled that day
- important decisions
- notable observations
- follow-up items

Only promote cross-day, reusable information into `MEMORY.md`.
